{
  "action": "evaluate",
  "args": {
    "code": "(() => { const btn = [...document.querySelectorAll('button')].find(b => b.textContent && b.textContent.includes('Create repository')); return btn ? {tag: btn.tagName, text: btn.textContent.trim().substring(0,30), id: btn.id, class: btn.className.substring(0,50)} : {found: false}; })()"
  },
  "session": "github2"
}
